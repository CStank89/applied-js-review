// Make a GET Request to the following url: https://picsum.photos/v2/list
// Make sure to examine the response first to see what the data looks like

// Create a function that will take in an object create the following:
/* 
    <div class='photo-card'>
        <div class='img-wrapper'>
            <img src={download url} />
        </div>
        <p> Author: {autho's name}</p>
        <p>Original Height: {img height}</p>
        <p>Original Width: {img width}</p>
    </div>
*/

// Use your function to create a crad for each of the images and append it
// to the div with the class name 'photo-container'